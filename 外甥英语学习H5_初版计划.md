# 外甥英语学习 H5 —— 初版计划

## 1. 项目定位

这是一个给外甥个人使用的英语学习手机端 H5，不以做成完整教育平台为目标。

核心目标：

> 让一个学习主动性较弱的初中生愿意打开网页，并在轻量、有趣的互动中完成英语学习。

核心体验：

```text
进入 H5
  ↓
选择玩法
  ↓
认识几个单词
  ↓
听发音
  ↓
跟读
  ↓
完成简单互动
  ↓
获得奖励
  ↓
继续下一轮
```

---

## 2. 技术范围

第一版：

- Vue 3
- TypeScript
- Tailwind CSS
- Pinia（确实需要时再使用）
- LocalStorage / IndexedDB

数据直接放在前端项目中：

```text
src/
├── data/
│   ├── words.ts
│   ├── levels.ts
│   └── ...
├── audio/
│   ├── words/
│   └── ...
├── views/
│   ├── Home.vue
│   ├── Word.vue
│   └── Game.vue
└── ...
```

第一版不做：

- 后端
- 数据库
- 登录注册
- 用户系统
- 管理后台
- 服务端 API
- 云端学习数据同步

---

## 3. 第一版核心功能

### 3.1 首页

首页不要做成传统的课程/功能导航，而是更偏游戏入口。

例如：

```text
┌──────────────────────┐
│                      │
│     🐶 English       │
│                      │
│      Lv. 3           │
│      ⭐ 320           │
│                      │
│  ┌────────────────┐  │
│  │   🎮 开始学习   │  │
│  └────────────────┘  │
│                      │
│  今日进度             │
│  ⭐⭐⭐⭐⭐             │
│                      │
└──────────────────────┘
```

第一版可以只做：

- 等级
- 星星
- 开始学习
- 今日进度

后续再增加宠物、地图、成就等。

---

## 4. 单词学习

这是第一阶段最重要的功能。

一个单词展示：

- 单词
- 音标
- 中文释义
- 词性（可选）
- 简单例句
- 单词发音
- 跟读入口

示例：

```text
┌──────────────────────┐
│                      │
│       important      │
│                      │
│      /ɪmˈpɔːr.../    │
│                      │
│         🔊           │
│                      │
│        重要的         │
│                      │
│  This is important.  │
│                      │
│      🎙 跟我读        │
│                      │
└──────────────────────┘
```

第一版不要塞太多内容：

- 词根
- 词缀
- 大量释义
- 近义词/反义词
- 复杂语法说明

先把「单词 → 发音 → 中文 → 例句 → 跟读」做好。

---

## 5. 音频方案

音频分两个阶段。

### 5.1 第一阶段：浏览器 TTS

开发初期直接使用 Web Speech API 的 `SpeechSynthesis`。

```js
const speak = (text) => {
  speechSynthesis.cancel()

  const utterance = new SpeechSynthesisUtterance(text)

  utterance.lang = 'en-US'
  utterance.rate = 0.8
  utterance.pitch = 1

  speechSynthesis.speak(utterance)
}
```

第一阶段优点：

- 不需要准备音频文件
- 不需要服务器
- 不需要数据库
- 不需要 API Key
- 单词和句子都可以直接播放
- 适合快速验证 H5 的交互

注意：

不同手机、浏览器/WebView 使用的语音引擎可能不同，因此更适合 MVP 和开发阶段。

---

### 5.2 第二阶段：AI/TTS 批量生成音频

等单词数据稳定以后，批量生成 MP3：

```text
words.json
    ↓
TTS
    ↓
批量生成 MP3
    ↓
audio/
├── apple.mp3
├── banana.mp3
├── important.mp3
└── beautiful.mp3
```

前端统一通过一个音频方法播放：

```js
playAudio(word.audio)
```

推荐优先尝试：

**edge-tts**

它适合用脚本批量生成单词和句子的音频。

例如：

```bash
pip install edge-tts
```

批量流程：

```text
100 个单词
    ↓
edge-tts
    ↓
100 个 mp3
    ↓
放进 H5 静态资源
```

以后句子也使用相同方式生成。

---

### 5.3 音频模块设计原则

前端不要把 TTS 实现方式写死在页面里。

统一封装：

```text
playAudio("apple")
```

现在内部可以使用：

```text
SpeechSynthesis
```

以后替换为：

```text
/audio/apple.mp3
```

这样页面和学习逻辑不需要修改。

---

## 6. 跟读功能

### V1

先实现基础跟读：

```text
🔊 听标准发音
       ↓
🎙 开始跟读
       ↓
录音结束
       ↓
▶ 听自己的声音
       ↓
完成
```

第一版暂时不判断发音是否标准。

### V2

后续再加入语音识别：

```text
🎙 Apple
   ↓
语音识别
   ↓
识别结果："apple"
   ↓
🎉 读对了
```

第一版不要直接做复杂的 AI 发音评分。

---

## 7. 趣味性设计

这是这个项目非常重要的一部分。

不要让用户感觉是在：

> 「今天背 20 个单词」

而是：

> 「完成一关」

例如：

```text
🍎 水果岛

第 1 关

apple
banana
orange
```

完成后：

```text
🎉 关卡完成

⭐ +30
```

一次只处理 3～5 个单词，降低心理压力。

---

## 8. 奖励系统

第一版保持简单。

```text
完成单词      +5 ⭐
跟读完成      +5 ⭐
答对小游戏    +10 ⭐
完成关卡      +30 ⭐
```

等级可以简单按星星累计：

```text
0~100       Lv.1
100~250     Lv.2
250~500     Lv.3
...
```

奖励的重点是鼓励参与，而不是单纯强调成绩。

---

## 9. 小游戏

第一版先做 2～3 个，不要铺太多。

### 游戏 A：听音选词

```text
🔊

Which word?

┌─────────┐
│ apple   │
└─────────┘

┌─────────┐
│ apply   │
└─────────┘

┌─────────┐
│ April   │
└─────────┘
```

### 游戏 B：单词配对

例如：

```text
apple       苹果
banana      香蕉
orange      橙子
```

点击对应的英文和中文进行匹配。

### 游戏 C：简单打怪

```text
        👹

       HP 30

   apple   banana
   orange  animal
```

答对：

```text
💥 -10 HP
```

三次答对：

```text
🎉 KO！
```

本质上仍然是选择题，只是换成游戏包装。

---

## 10. 单词数据

建议一开始就预留音频和例句字段：

```ts
interface Word {
  id: string
  word: string
  phonetic?: string
  meaning: string
  example?: string
  exampleMeaning?: string

  audio?: string
  exampleAudio?: string

  level?: number
  category?: string
}
```

第一阶段：

```text
audio = 浏览器 TTS
```

以后：

```text
audio = /audio/apple.mp3
```

这样不用修改学习页面。

第一批建议只准备 50～100 个单词，先把完整体验跑通，再扩大词汇量。

---

## 11. 本地学习记录

第一版使用 LocalStorage 即可。

可以保存：

```json
{
  "stars": 320,
  "level": 3,
  "learnedWords": ["apple", "banana"],
  "completedLevels": ["fruit-1"],
  "streak": 3
}
```

如果以后学习记录复杂，再考虑 IndexedDB。

目前不需要后端。

---

## 12. 后续功能路线

### 第一阶段：先做出来

```text
① 手机 H5 UI
② 单词数据
③ 浏览器 TTS
④ 单词学习
⑤ 跟读录音
⑥ LocalStorage
⑦ 简单奖励
```

### 第二阶段：增加趣味性

```text
① 关卡
② 星星 / 等级
③ 小游戏
④ 连续学习
⑤ 宠物 / 地图 / 收集
```

### 第三阶段：提升音频质量

```text
浏览器 TTS
    ↓
edge-tts 批量生成
    ↓
MP3
    ↓
H5 静态资源
```

同时生成：

- 单词音频
- 句子音频
- 游戏提示音

### 第四阶段：增加 AI 能力

```text
语音识别
    ↓
判断是否读对
```

以及：

```text
AI
 ↓
根据单词生成
例句 / 选择题 / 拼写题
```

---

## 13. 暂时不做

以下内容暂时放入 TODO，不进入初版：

```text
❌ 后端
❌ 数据库
❌ 登录
❌ 用户体系
❌ 管理后台
❌ 在线题库
❌ 云端同步
❌ 复杂学习算法
❌ 爬商业网站音频 API
❌ 自己部署大型 TTS 模型
❌ AI 发音精确评分
```

这些如果以后确实需要，再单独研究。

---

## 14. 推荐的 MVP 形态

第一版最终可以非常简单：

```text
                 🐶
            English Adventure

                  Lv.3
                ⭐ 320

             [ 开始冒险 ]

                  ↓

          🍎 第 1 关：水果

                  ↓

              apple

                 🔊

              苹果

            🎙️ 跟我读

                  ↓

          🎉 完成 +5 ⭐

                  ↓

            下一个单词

                  ↓

          3 个单词完成

             🎮 小游戏

                  ↓

              +20 ⭐

                  ↓

             下一关
```

---

## 15. 核心开发原则

### 原则 1：先验证“他愿不愿意玩”

这个项目的第一指标不是：

> 学了多少单词。

而是：

> **他愿不愿意主动打开第二次。**

### 原则 2：先小后大

先：

```text
50 个单词
+
1 个关卡
+
1 个小游戏
```

跑通完整流程。

不要一开始准备几千个单词。

### 原则 3：音频实现解耦

页面只关心：

```text
playAudio(text)
```

底层可以从浏览器 TTS 切换到批量 MP3。

### 原则 4：游戏化服务于学习

小游戏本质上还是让他：

- 听
- 看
- 读
- 认
- 拼写

只是把这些动作包装得更有趣。

### 原则 5：先不考虑后端

所有数据：

```text
前端 JSON
+
LocalStorage
```

足够支撑第一版。

---

## 16. 第一阶段建议的实际开发顺序

```text
Day 1
└── 搭建 Vue 3 H5 + 手机端基础 UI

Day 2
└── 单词数据 + 单词卡片

Day 3
└── SpeechSynthesis 播放

Day 4
└── 跟读录音 + 播放自己的录音

Day 5
└── LocalStorage + 星星/等级

Day 6
└── 第一关 + 简单小游戏

Day 7
└── 给外甥实际试玩，根据反馈调整
```

这份计划的核心是：

> **先做一个真的能玩的东西，而不是先做一个“完整的平台”。**
